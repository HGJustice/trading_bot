use crate::backend::*;

// helper function to find the lowest close in 48 hours


// helper function to get current two candles to compare to the lowest 


// function to execute long if above coniditons are met 